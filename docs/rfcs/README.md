# Relevant RFCs

The documents are gzipped, to read them either do `gunzip GZ_FILE` or use a gzip compatible reader-editor, like vim (see `:help gzip`):

1. open vim
2. `:set loaded_gzip = 1`
3. `:e GZ_FILE`
